name: acs26-stage06

services:

  # PostgreSQL database used by ACS to store metadata,
  # users, permissions, workflow state, and configuration.
  postgres:
    image: postgres:${POSTGRES_TAG}

  # OpenSearch stores and serves ACS search indexes.
  opensearch:
    image: opensearchproject/opensearch:${OPENSEARCH_TAG}

  # ActiveMQ provides messaging between ACS and supporting services.
  activemq:
    image: quay.io/alfresco/alfresco-activemq:${ACTIVEMQ_TAG}

  # Shared temporary storage used by transformation services.
  shared-file-store:
    image: quay.io/alfresco/alfresco-shared-file-store:${SHARED_FILE_STORE_TAG}

  # Performs document transformations, preview generation,
  # thumbnail creation, and text extraction.
  transform-core-aio:
    image: quay.io/alfresco/alfresco-transform-core-aio:${TRANSFORM_CORE_AIO_TAG}

  # Routes transformation requests to available transform engines.
  transform-router:
    image: quay.io/alfresco/alfresco-transform-router:${TRANSFORM_ROUTER_TAG}

  # Alfresco Content Repository.
  # Core content services platform.
  alfresco:
    image: quay.io/alfresco/alfresco-content-repository:${ACS_TAG}

    environment:
      JAVA_OPTS: >-

        # OpenSearch integration.
        -Dindex.subsystem.name=elasticsearch
        -Delasticsearch.host=opensearch

        # Messaging configuration.
        -Dmessaging.broker.url="failover:(nio://activemq:61616)?timeout=3000&jms.useCompression=true"

        # Transformation services.
        -Dtransform.service.url=http://transform-router:8095

        # NEW IN STAGE 06
        # Enable browser access from external web applications.
        -Dcors.enabled=true

        # Allow requests from Workspace, Share, and Control Center.
        -Dcors.allowed.origins=http://${NGINX_SERVER_NAME:-localhost}:${ADW_HTTP_PORT},http://${NGINX_SERVER_NAME:-localhost}:${CONTROL_CENTER_HTTP_PORT},http://${NGINX_SERVER_NAME:-localhost}:${SHARE_HTTP_PORT}

        # Allowed HTTP methods.
        -Dcors.allowed.methods=GET,POST,PUT,DELETE,OPTIONS,HEAD

        # Allowed request headers.
        -Dcors.allowed.headers=Authorization,Content-Type,Cache-Control,Accept,Origin,X-Requested-With,Access-Control-Request-Method,Access-Control-Request-Headers

        # Support browser credentials.
        -Dcors.support.credentials=true

  # Keeps OpenSearch synchronized with repository changes.
  search-live-indexing:
    image: quay.io/alfresco/alfresco-elasticsearch-live-indexing:${SEARCH_LIVE_INDEXING_TAG}

  # Performs initial repository indexing.
  search-reindexing:
    image: quay.io/alfresco/alfresco-elasticsearch-reindexing:${SEARCH_REINDEXING_TAG}

  # NEW IN STAGE 06
  # Alfresco Digital Workspace (ADW).
  # Modern Angular-based user interface for ACS.
  digital-workspace:
    image: quay.io/alfresco/alfresco-digital-workspace:${ADW_TAG}
    depends_on:
      - alfresco

    ports:
      # User-facing web application.
      - "${ADW_HTTP_PORT}:8080"

    environment:
      # Connect ADW to ACS.
      APP_CONFIG_PROVIDER: ECM
      APP_CONFIG_AUTH_TYPE: BASIC

      # URL users access through the browser.
      APP_CONFIG_ECM_HOST: http://${NGINX_SERVER_NAME:-localhost}:${ADW_HTTP_PORT}

      # Disable OAuth for simplicity in training.
      APP_CONFIG_OAUTH2_SILENT_LOGIN: "false"
      APP_CONFIG_OAUTH2_CODE_FLOW: "false"

      # Disable APS integration.
      APP_CONFIG_PLUGIN_PROCESS_SERVICE: "false"

  # NEW IN STAGE 06
  # Administrative interface for monitoring,
  # troubleshooting, and managing ACS.
  control-center:
    image: quay.io/alfresco/alfresco-control-center:${CONTROL_CENTER_TAG}
    depends_on:
      - alfresco

    ports:
      - "${CONTROL_CENTER_HTTP_PORT}:8080"

    environment:
      APP_CONFIG_PROVIDER: ECM
      APP_CONFIG_AUTH_TYPE: BASIC

      # Connect to ACS.
      APP_CONFIG_ECM_HOST: http://${NGINX_SERVER_NAME:-localhost}:${REPO_HTTP_PORT}

  # NEW IN STAGE 06
  # Traditional Alfresco Share user interface.
  # Provides Sites, Document Library, Rules, and legacy UI features.
  share:
    image: quay.io/alfresco/alfresco-share:${SHARE_TAG}
    depends_on:
      - alfresco

    ports:
      - "${SHARE_HTTP_PORT}:8080"

    environment:
      # Connect Share to ACS.
      REPO_HOST: alfresco
      REPO_PORT: 8080
