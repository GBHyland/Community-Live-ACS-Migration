# -------------------------------------------------------------------
# Stage 02 - Integrated Content Transformation Services
#
# This Docker Compose file expands the foundational ACS repository
# environment by adding Transform Core AIO services.
#
# The environment now includes:
# - PostgreSQL database
# - Alfresco Content Services Repository
# - Transform Core AIO
#
# This stage introduces document transformation capabilities such as:
# - PDF previews
# - Thumbnail generation
# - Renditions
# - Document format conversions
#
# Search/indexing, messaging infrastructure, user interfaces,
# reverse proxy routing, and operational hardening are still
# introduced in later stages.
# -------------------------------------------------------------------

name: acs26-stage02

services:

  # PostgreSQL database used by Alfresco to store repository metadata,
  # permissions, node information, and system configuration data.
  postgres:
    image: postgres:${POSTGRES_TAG}
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

  # Transform Core AIO service used for content transformations.
  # Provides previews, thumbnails, renditions, and document
  # conversion capabilities for repository content.
  transform-core-aio:
    image: quay.io/alfresco/alfresco-transform-core-aio:${TRANSFORM_CORE_AIO_TAG}

  # Core Alfresco Content Services repository application.
  # Provides content management APIs, repository services,
  # content storage management, and connects to PostgreSQL
  # and Transform Core AIO.
  alfresco:
    image: quay.io/alfresco/alfresco-content-repository:${ACS_TAG}
    depends_on:
      - postgres
      - transform-core-aio
    ports:
      - "${REPO_HTTP_PORT}:8080"
    environment:
      JAVA_TOOL_OPTIONS: >-
        -Dencryption.keystore.type=JCEKS
        -Dencryption.cipherAlgorithm=DESede/CBC/PKCS5Padding
        -Dencryption.keyAlgorithm=DESede
        -Dencryption.keystore.location=/usr/local/tomcat/shared/classes/alfresco/extension/keystore/keystore
        -Dmetadata-keystore.password=${METADATA_KEYSTORE_PASSWORD}
        -Dmetadata-keystore.aliases=metadata
        -Dmetadata-keystore.metadata.password=${METADATA_KEYSTORE_METADATA_PASSWORD}
        -Dmetadata-keystore.metadata.algorithm=DESede
      JAVA_OPTS: >-
        -Ddb.driver=org.postgresql.Driver
        -Ddb.username=${POSTGRES_USER}
        -Ddb.password=${POSTGRES_PASSWORD}
        -Ddb.url=jdbc:postgresql://postgres:5432/${POSTGRES_DB}
        -Dindex.subsystem.name=noindex
        -Dalfresco.events.include=
        -Dmessaging.subsystem.autoStart=false
        -DlocalTransform.core-aio.url=http://transform-core-aio:8090/
        -Dcsrf.filter.enabled=false
        -Ddeployment.method=DOCKER_COMPOSE
        -Dalfresco.host=localhost
        -Dalfresco.port=${REPO_HTTP_PORT}
        -Dalfresco.protocol=http
